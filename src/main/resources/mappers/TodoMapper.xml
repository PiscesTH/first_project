<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clean.cleanssakssak.todo.TodoMapper">
    <insert id="insTodo" useGeneratedKeys="true" keyProperty="todoId">
        INSERT INTO todo
        SET user_id = #{userId},
            cleaning = #{cleaning},
            d_day = #{dDay},
            is_checked = #{isChecked},
            is_del = #{isDel}
    </insert>
    
    <select id="selTodoAll">
        SELECT todo_id AS todoId, cleaning, d_day AS dDay
        FROM todo
        WHERE user_id = #{loginedUserId}
        LIMIT #{startdIdx}, #{rowCount}
    </select>
</mapper>